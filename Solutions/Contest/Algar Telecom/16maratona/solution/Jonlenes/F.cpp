#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <algorithm>
#include <string>
#include <stdio.h>
#include <stdlib.h>
#include <vector>
#include <set>

using namespace std;

int main() {
  freopen("in.txt", "r", stdin);
  //freopen("out.txt", "w", stdout);

  int n;

  while (cin >> n && n) {
    vector<int> vt1, vt2, vt3;
    for (int i = 0; i < n; ++i) {
      cin >> t;
      vt1.push_back(t1);
    }

    for (int i = 0; i < n; ++i) {
      cin >> t;
      vt2.push_back(t1);
    }

    for (int i = 0; i < n; ++i) {
      cin >> t;
      vt3.push_back(t1);
    }



  }

  return 0;
}
